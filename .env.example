# Phase 1 runs without secrets.
# Add future API credentials here and keep real values in .env.local only.

# Optional future GitHub token for higher rate limits.
# GITHUB_TOKEN=

# Optional future email provider key for contact form delivery.
# CONTACT_EMAIL_API_KEY=
